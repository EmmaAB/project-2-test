##rename this file config.py because config is imported in app.py
##Input your username and password. Update the postgres port if it varies from the default 5432
db_username = "postgres_username"
db_password = "postgres_password"
postgres_port = "5432"

